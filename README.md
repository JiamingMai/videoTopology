# Discription
This is the first version of the prototype topology which is designed to call the caffe framework. Note that the script "server.py" is used to run in the local machine, and it plays the role of invoking the caffe framework and getting back the extracted features from caffe.
